// #################################################################################################
//  ALib - A-Worx Utility Library
//
//  (c) 2013-2016 A-Worx GmbH, Germany
//  Published under MIT License (Open Source License, see LICENSE.txt)
// #################################################################################################
/** @file */ // Hello Doxyen

// to preserve the right order, we are not includable directly from outside.
#if !defined(FROM_HPP_AWORX_LIB_ALIB) || defined(HPP_AWORX_LIB_ALIB_SYMBOLS)
    #error "include alib/alib.hpp instead of this header"
#endif


#ifndef HPP_AWORX_LIB_ALIB_SYMBOLS
#define HPP_AWORX_LIB_ALIB_SYMBOLS 1


/**
 * @defgroup GrpALibCompilerSymbols ALib Compilation Symbols
 *
 * Symbols (C++ preprocessor macros) listed here, support adding or removing features to ALib when
 * compiling the library and any code unit that uses ALib.
 * These symbols must be used <em>only within makefiles or IDE project files to be passed to the
 * C++ compiler (!) </em> (normally -D option). In other words: they must not be defined or undefined
 * within the source code that uses ALib.
 *
 * ALib Symbols are prefixed with <em>ALIB_</em>.
 *
 * For most of the symbols, a version with a postfix <em>_ON</em> and for some also one with the postfix
 * <em>_OFF</em> exists. These symbol decide about the definition, respectively the avoidance of the
 * definition of a corresponding <em>code selection symbol</em> that is named the same without the
 * postfix.<br>
 * If both are set at the same time, a compiler error is generated by ALib header files.
 * If none of them is passed to the compiler, a default value is assumed.<p>
 *
 * As a convenience, to get default behavior, no symbol needs to be passed to the compiler.<p>
 *
 * \note Some of the compilation symbols are changing the definition of classes and methods which
 *       makes binaries incompatible with binaries that do not use the same setting for each of
 *       these symbols.<br>
 *       Symbols that have to be kept equal across compilation units are denoted in this
 *       documentation.
 * @{ @}
*/

/**
 * @defgroup GrpALibCodeSelectorSymbols ALib Symbols To Select Code Fragments
 *
 *  Symbols (C++ preprocessor macros) listed here, are mostly generated out of
 * \ref GrpALibCompilerSymbols. In this case, their name corresponds to these symbols, excluding
 * the postfixes <em>_ON</em> and <em>_OFF</em>.<p>
 *
 * These symbols must not be passed to the compiler and must also not be set manually from within
 * code entities that use ALib libraries. They can be used in code entities that use ALib to select
 * code depending on the same settings of ALib compilation. Selection takes place,
 * using preprocessor directives:<p>
 * \code{.cpp}
 * #if defined(ALIB_XYZ_SELECTOR)
 *   // your selective code
 * #endif
 * \endcode
 *
 * \note Besides ALib-specific symbols of that type, ALib internally uses also platform specific symbols
 * to select code, e.g. __GNUC__, _MSC_VER to detect compilers and __GLIBCXX__, _WIN32 to detect
 * libraries used.
 *
 * ALib Symbols are prefixed with <em>ALIB_</em>.<p>
 * @{ @}
 */


// #################################################################################################
// ALIB_ Symbols
// #################################################################################################
/**
 * @addtogroup GrpALibCompilerSymbols
 * @{ \def  ALIB_GTEST_ON
 *  This compiler symbol is passed to the compiler to define code selection symbol
 *  \ref ALIB_GTEST which selects unit test code in accordance to the Google Test libraries.
 *  In respect to the selection of the test library, this symbol has priority over
 *  \ref ALIB_VSTUDIO_ON.
 * @}
 *
 * @addtogroup GrpALibCodeSelectorSymbols
 * @{ \def  ALIB_GTEST
 *  Selects unit test code in accordance to the Google Test libraries.
 *  Use \ref ALIB_GTEST_ON to define this symbol.
 * @}
 */
#if defined(ALIB_GTEST)
    #error "ALIB_GTEST must not be set from outside. Use ALIB_GTEST_ON instead!"
#endif

#if defined(ALIB_GTEST_ON)
    #define ALIB_GTEST
#endif

/**
 * @addtogroup GrpALibCompilerSymbols
 * @{ \def  ALIB_ECLIPSE_CDT_ON
 *  This compiler symbol is passed to the compiler to define code selection symbol
 *  \ref ALIB_ECLIPSE_CDT. This symbol should be set in all Eclipse CDT projects
 *  which include ALib.<p>
 * @}
 *
 * @addtogroup GrpALibCodeSelectorSymbols
 * @{ \def  ALIB_ECLIPSE_CDT
 *  Used to select code fragments which are specific to running (and debugging) software from within
 *  the IDE Eclipse with CDT.<p>
 * @}
 */
#if defined(ALIB_ECLIPSE_CDT)
    #error "ALIB_ECLIPSE_CDT must not be set from outside. Use ALIB_ECLIPSE_CDT_ON instead!"
#endif

#if defined(ALIB_ECLIPSE_CDT_ON)
    #define ALIB_ECLIPSE_CDT
#endif

/**
 * @addtogroup GrpALibCompilerSymbols
 * @{ \def  ALIB_QTCREATOR_ON
 *  This compiler symbol is passed to the compiler to define code selection symbol
 *  \ref ALIB_QTCREATOR. This symbol should be set in all QT Creator project files
 *  which include ALib.<p>
 * @}
 *
 * @addtogroup GrpALibCodeSelectorSymbols
 * @{ \def  ALIB_QTCREATOR
 *  Used to select code fragments which are specific to running (and debugging) software from within
 *  the IDE QT Creator.<p>
 * @}
 */
#if defined(ALIB_QTCREATOR)
    #error "ALIB_QTCREATOR must not be set from outside. Use ALIB_QTCREATOR_ON instead!"
#endif

#if defined(ALIB_QTCREATOR_ON)
    #define ALIB_QTCREATOR
#endif

/**
 * @addtogroup GrpALibCompilerSymbols
 * @{ \def  ALIB_VSTUDIO_ON
 *  This compiler symbol is passed to the compiler to define code selection symbol
 *  \ref ALIB_VSTUDIO. This symbol should be set in all Microsoft Visual Studio project
 *  files which include ALib.<p>
 *  \note In respect to the selection of the unit testing framework, the symbol
 *  \ref ALIB_GTEST_ON supersedes this symbol.
 * @}
 *
 * @addtogroup GrpALibCodeSelectorSymbols
 * @{ \def  ALIB_VSTUDIO
 *  Used to select code fragments which are specific to running (and debugging) software from within
 *  Microsoft Visual Studio.<p>
 *  For example, ALib unit tests are using this symbol to select the Visual Studio unit test framework.
 *  Use \ref ALIB_VSTUDIO_ON to define this symbol.
 * @}
 */

#if defined(ALIB_VSTUDIO)
    #error "ALIB_VSTUDIO must not be set from outside. Use ALIB_VSTUDIO_ON instead!"
#endif

#if defined(ALIB_VSTUDIO_ON)
    #define ALIB_VSTUDIO
#endif



// #################################################################################################
// ALIB_DLL_EXPORTS
// #################################################################################################
/**
 * @addtogroup GrpALibCompilerSymbols
 * @{ \def  ALIB_DLL_EXPORTS
 *  This compiler symbol has to be defined when compiling ALib classes into a DLL under
 *  Windows/MSC.
 *  Within code units that use a DLL that contains ALib, this symbol must not be defined.
 * @}
 */


/**
 * @addtogroup GrpALibCompilerSymbols
 * @{ \def  ALIB_DEBUG_ON
 *  This compiler symbol forces ALib to compile in debug mode by defining the code selection symbol
 *  \ref ALIB_DEBUG. Plausibility and assertion checks are performed in this mode.<br>
 *  If neither this symbol nor \ref ALIB_DEBUG_OFF is provided, then ALib headers rely on
 *  the symbol <b>NDEBUG</b>. If NDEBUG is given, \ref ALIB_DEBUG is not set. Otherwise it is.
 * @}
 *
 * @addtogroup GrpALibCompilerSymbols
 * @{ \def  ALIB_DEBUG_OFF
 *  This compiler symbol forces ALib to compile without internal plausibility and assertion checks,
 *  by leaving the code selection symbol \ref ALIB_DEBUG undefined.
 *  If neither this symbol nor \ref ALIB_DEBUG_OFF is provided, then ALib headers rely on
 *  a set of standard symbols introduced (and set) by other libraries, e.g. <em>NDEBUG</em>.
 * @}
 *
 * @addtogroup GrpALibCodeSelectorSymbols
 * @{ \def  ALIB_DEBUG
 *  If defined, plausibility checks and ALib assertions are enabled.
 *  See \ref ALIB_DEBUG_ON and \ref ALIB_DEBUG_OFF for information about how to manipulate
 *  this symbol.
 * @}
 */
#if defined(ALIB_DEBUG)
    #error "ALIB_DEBUG must not be set from outside"
#endif

#if defined(ALIB_DEBUG_OFF) && defined(ALIB_DEBUG_ON)
    #error "ALIB_DEBUG_OFF / ALIB_DEBUG_ON are both set"
#endif

#if !defined(ALIB_DEBUG_OFF)
    #if defined(ALIB_DEBUG_ON) || !defined(NDEBUG) || defined(_DEBUG) || defined(DEBUG)
        #define ALIB_DEBUG
    #endif
#endif

/**
 * @addtogroup GrpALibCompilerSymbols
 * @{ \def  ALIB_DEBUG_STRINGS_ON
 *  This compiler symbol enables additional debug code within class AString. When provided,
 *  it defines \ref ALIB_DEBUG_STRINGS. This is useful when extending or specifically debugging
 *  class AString.
 * @}
 *
 * @addtogroup GrpALibCodeSelectorSymbols
 * @{ \def  ALIB_DEBUG_STRINGS
 *  Selects extra debug code within class AString. Gets defined by compiler symbol
 *  \ref ALIB_DEBUG_STRINGS_ON.
 * @}
 */

#if defined(ALIB_DEBUG_STRINGS_OFF) && defined(ALIB_DEBUG_STRINGS_ON)
    #error "ALIB_DEBUG_STRINGS_OFF / ALIB_DEBUG_STRINGS_ON are both set"
#endif

#if defined( ALIB_DEBUG_STRINGS_ON )
    #define    ALIB_DEBUG_STRINGS
#endif


/**
 * @addtogroup GrpALibCompilerSymbols
 * @{ \def  ALIB_FEAT_THREADS_ON
 *  This compiler symbol enables the use of threads within ALib, by.
 *  defining source selection symbol \ref ALIB_FEAT_THREADS.
 *  This is the default, hence the symbol is not needed to be passed, but it is available for
 *  completeness.<br>
 *  See \ref ALIB_FEAT_THREADS_OFF for more information.
 * @}
 *
 * @addtogroup GrpALibCompilerSymbols
 * @{ \def  ALIB_FEAT_THREADS_OFF
 *  This compiler symbol disables the use of threads within ALib, by
 *  preventing the definition of source selection symbol \ref ALIB_FEAT_THREADS.
 *  This might be useful for platforms where no thread library is available or to minimize code
 *  size. In general, if unsure, do not use this symbol and use the multi-threaded version of ALib.<br>
 *  When this symbol is set, classes
 *  \ref aworx::lib::threads::Thread "Thread" and
 *  \ref aworx::lib::threads::Runnable "Runnable" are not available. However, the classes
 *  \ref aworx::lib::threads::ThreadLockNR "ThreadLockNR" and
 *  \ref aworx::lib::threads::ThreadLock "ThreadLock" are!
 *  Their interface remains the same, but they do not perform any locking. Due to this behavior,
 *  it is possible to write code that performs due locking while still, in single threaded
 *  compilation, the code does not suffer any performance drawbacks, because lock code  will be
 *  <em>pruned</em>.
 * @}
 *
 * @addtogroup GrpALibCodeSelectorSymbols
 * @{ \def  ALIB_FEAT_THREADS
 *  Selects code within ALib (or source entities that use ALib) that cares about threads and
 *  due locking of shared resources.
 *  See compiler symbol \ref ALIB_FEAT_THREADS_OFF for more information.
 * @}
 */


#if defined(ALIB_FEAT_THREADS)
    #error "ALIB_DEBUG must not be set from outside"
#endif

#if defined(ALIB_FEAT_THREADS_ON) && defined(ALIB_FEAT_THREADS_OFF)
    #error "ALIB_FEAT_THREADS_ON / ALIB_FEAT_THREADS_OFF are both set"
#endif

#if !defined(ALIB_FEAT_THREADS_OFF)
    #define ALIB_FEAT_THREADS
#endif



#endif // HPP_AWORX_LIB_ALIB_SYMBOLS
